"use client";import {FormEvent,useState}from"react";import{useRouter}from"next/navigation";import{ArrowRight}from"lucide-react";import{Project,request}from"@/lib/api";
export default function NewProject(){const router=useRouter();const[busy,setBusy]=useState(false);const[error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const form=new FormData(e.currentTarget);try{const p=await request<Project>("/api/projects",{method:"POST",body:JSON.stringify(Object.fromEntries(form))});router.push(`/projects/${p.id}/capture`)}catch(e){setError((e as Error).message);setBusy(false)}}return <main className="shell"><form className="card form" onSubmit={submit}><div className="eyebrow">Novo projeto</div><h1>O que vamos reconstruir?</h1><p className="sub">Podes alterar o nome e a descrição mais tarde.</p>{error&&<p className="error">{error}</p>}<div className="field"><label>Nome do projeto</label><input className="input" name="name" required minLength={2} placeholder="Ex.: Vaso de cerâmica"/></div><div className="field"><label>Descrição opcional</label><textarea className="input" name="description" rows={4} placeholder="Notas sobre o objeto e a captura"/></div><div className="field"><label>Tipo de captura</label><select className="input" name="capture_type"><option value="small_object">Objeto pequeno</option><option value="medium_object">Objeto médio</option><option value="environment">Espaço ou ambiente</option></select></div><button className="btn primary" disabled={busy}>{busy?"A criar…":"Criar e adicionar imagens"}<ArrowRight size={17}/></button></form></main>}
